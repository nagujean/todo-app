# SMTP Configuration for sending invitation emails
# Copy this file to .env and fill in your SMTP credentials

# SMTP Server Host (e.g., smtp.gmail.com, smtp.sendgrid.net)
SMTP_HOST=smtp.gmail.com

# SMTP Server Port (465 for SSL, 587 for TLS)
SMTP_PORT=587

# SMTP Username (your email address for Gmail)
SMTP_USER=your-email@gmail.com

# SMTP Password or App Password
# For Gmail, use an App Password: https://support.google.com/accounts/answer/185833
SMTP_PASS=your-app-password

# From Email Address (displayed as sender)
SMTP_FROM="Todo App <noreply@your-domain.com>"

# Application URL (used for generating invitation links)
APP_URL=https://your-app-url.vercel.app
