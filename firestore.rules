rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ============================================
    // 개발용 간소화 규칙 (Development Mode)
    // 인증된 모든 사용자에게 전체 읽기/쓰기 권한 부여
    // ============================================

    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
